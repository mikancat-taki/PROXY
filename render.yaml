services:
  - type: web
    name: proxy-browser
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "npm run start"
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      -
